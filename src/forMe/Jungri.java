package forMe;
//2025.12.31.
public class Jungri {

	// ✅ 용어 정리 (헷갈리기 좋은 것들 정리해둔 주석임)

	// 1) 멤버변수 = 필드 = 인스턴스 변수
	// - 클래스 안에 선언된 변수 중에서, 객체(인스턴스)가 각각 따로 가지고 있는 변수.
	// - 예) Bus 클래스 안의 busNumber, passengerCount, money 같은 것들.
	// - 그래서 아래 세 표현은 거의 같은 걸 가리킨다고 보면 됨.
	// * 멤버변수
	// * 필드(field)
	// * 인스턴스 변수(instance variable)

	// 2) 인스턴스 = 객체
	// - 'new' 키워드를 사용해서 클래스로부터 실제로 만들어진 실체를 말함.
	// 예) Bus bus100 = new Bus(100);
	//	   여기서 new Bus(100); 으로 만들어진 것이 Bus 클래스의 '객체'이자 '인스턴스'.
	// - 그래서 보통 'Bus 객체', 'Bus 인스턴스'라고 부르는 건 거의 같은 말.

	// 3) 참조 변수(레퍼런스 변수)
	// - 만들어진 객체(인스턴스)를 가리키는 변수 이름.
	// 예) Bus bus100 = new Bus(100);
//	       여기서 'bus100'이 참조 변수임.
	// - 흔히 "객체 이름"이라고 부르는 게 사실은 이 참조 변수 이름을 말하는 경우가 많음.

	// 4) 객체 이름(변수 이름)은 문자로 시작해야 함
	// - 자바에서 변수/객체 이름은 '식별자' 규칙을 따라야 함.
	// * 숫자로만 된 이름은 안 됨. → 100, 999 이런 건 '이름'이 아니라 그냥 숫자 값(int 리터럴).
	// * 첫 글자는 문자(A~Z, a~z), 언더스코어(_), 달러기호($)만 가능.
	// * 그 뒤부터는 숫자를 섞어 쓸 수 있음.
//	     예) bus100, bus999, _100 등은 가능하지만,
//	         100, 999 처럼 숫자만 단독으로 이름으로는 불가능.
	// - 그래서 '객체는 문법 규칙 때문에 문자 형태(식별자 규칙 지킨 이름)로만 이름을 지을 수 있다'라고 이해하면 됨.
	// 실제 값이 숫자(버스 번호 100, 999 등)인 건 필드(int busNumber)에 저장하는 거고,
	// 그걸 가리키는 변수 이름은 bus100, bus999 같은 식으로 문자+숫자 조합으로 지어야 함.

	// 5) 클래스 vs 객체 vs 인스턴스 정리
	// - 클래스(Class): 설계도.
	// 예) Bus, StdInfo 같은 타입 정의.
	// - 객체(Object): 설계도로 만들어낸 실제 물건.
	// 예) new Bus(100) 으로 생성된 것 하나하나가 객체.
	// - 인스턴스(Instance): '특정 클래스의 객체'라는 의미를 강조할 때 쓰는 말.
	// 예) Bus 클래스의 인스턴스 = Bus 객체.
	// - 참조 변수: 그 인스턴스를 가리키는 이름.
	// 예) Bus bus100 = new Bus(100); 에서 bus100이 참조 변수.
	
	
	
    // ========== Getter/Setter 메서드 ==========
    // 이클립스 자동완성: 우클릭 > Source > Generate Getters and Setters
    // 또는 Alt + Shift + S → R 단축키 사용
	
    // 오버로딩 생성자
    // 매개변수의 개수나 타입이 다르면 같은 이름의 생성자를 여러 개 만들 수 있음(오버로딩)
    // 이클립스 자동완성: 우클릭 > Source > Generate Constructor using Fields 선택
    // 또는 Alt + Shift + S → O 단축키 사용
	
	
	
//	
//	지금까지 이해하신 내용을 정리하면 대략 이렇게 정리할 수 있습니다:
//		1. 필드(멤버변수) 초기화
//		    클래스의 필드는 생성자가 어떻게 생겼든 관계 없이 객체가 만들어질 때 자동으로 기본값으로 초기화된다.
//		        숫자형(int, float 등): 0, 0.0
//		        boolean: false
//		        참조형(String, 배열 등): null
//		2. 기본 생성자와 오버로딩 생성자
//		    기본 생성자만 있을 때
//		        필드는 자동으로 기본값(0, null 등)으로 초기화되고,
//		        나중에 코드에서 person.name = "홍길동";처럼 직접 대입해서 값을 채우면 된다.
//		        이 경우에도 오류는 나지 않는다.
//		    오버로딩 생성자를 만들 때
//		        필드 자체는 여전히 기본값으로 먼저 초기화되지만,
//		        “객체를 만들면서 동시에 내가 원하는 값으로 시작하게 하고 싶기 때문에”
//		        this.name = name; this.height = height; 같은 초기화 코드를 생성자 안에 따로 적어준다.
//		        즉, “오버로딩이라서 자동 초기화가 안 되는 것”이 아니라,
//		        “기본값(0, null)이 아니라 매개변수로 받은 값으로 다시 덮어쓰는 것”이다.
//		3. getter/setter와 접근 제어
//		    필드를 public으로 두면 같은 패키지 안에서 person.name처럼 직접 접근할 수 있어서, getter/setter 없이도 동작은 한다.
//		    다만 객체지향 스타일로는 보통
//		        필드는 private으로 숨기고,
//		        외부에서는 getName(), setName() 같은 메서드로만 접근하게 만드는 쪽을 권장한다.
	
	
	
	
	
	
	
	
	
	
	
	
	
	
	
	
	
}
